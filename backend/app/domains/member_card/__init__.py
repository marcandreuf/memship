"""Member card domain — digital membership card + QR check-in (v0.7.0).

No models of its own: the card, QR, and PDF derive from existing member,
person, and organization data. Member numbering config lives on
``organization_settings``.
"""
