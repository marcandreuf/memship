"""Simple Bookings domain — spaces, admin-defined recurring weekly slots,
and member bookings with per-slot capacity and a FIFO waitlist.

Scaffold only; implemented across Stages 1–4 (see the Simple Bookings
implementation plan). Nothing here is wired into the app yet.
"""
