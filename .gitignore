# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
dist/
build/
.eggs/
*.whl
.venv/
venv/

# uv
.uv/

# Node / Frontend
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo

# pnpm
.pnpm-store/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.coverage
htmlcov/
.pytest_cache/
coverage.xml

# Rendered email previews (scripts/preview_emails.py)
.preview/

# Docker
docker-compose.override.yml

# OS
Thumbs.db
.DS_Store

# Logs
*.log
logs/

# Backups
backups/

# File uploads (local storage)
backend/storage/
/storage/

# Cypress
e2e/cypress/screenshots/
e2e/cypress/videos/
e2e/cypress/downloads/
e2e/cypress/reports/
e2e/runner-results/
# Rewritten by every `cypress-parallel` run, so it is dirty after any suite run.
# It only balances specs across workers, and a missing file falls back to equal
# weights and regenerates itself on the first run.
e2e/cypress/parallel-weights.json

# Claude Code session journals (local, never committed)
.claude/journals/

# Per-repo config for personal agent skills (local, never committed)
# The skills read these from the working tree; contributors do not need them.
docs/agents/

# Written on the server by scripts/install.sh at first install
POST-INSTALL.md
